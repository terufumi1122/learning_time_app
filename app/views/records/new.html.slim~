.container.text-center
  .row.d-flex.align-items-center
    .col-
      = render 'layouts/profile'
    .col-9
      .row
        - if @current_user.present?
          - if @user_records.last
            .col-4
              = render 'records/summary'
            .col-8
              = render 'layouts/btn'
          - else
            .col-4
              h1 Click Here =>
            .col-8
              = render 'layouts/btn'
        - else
          .col-4
            h1 Welcome to the World of Learning
            p this is the best app for you!
            = link_to 'Log in', login_path, class: 'btn btn-primary btn-lg m-1'
            = link_to 'Sign up', signup_path, class: 'btn btn-warning btn-lg m-1'
          .col-8
            p ゆくゆくデモGIFを配置
